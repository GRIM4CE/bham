version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 20
        - node -v && npm -v
        - cd ../../ && npm ci
    build:
      commands:
        - cd ../../ && npx turbo run build --filter=@bham/portfolio
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - ../../node_modules/**/*
