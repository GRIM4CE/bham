version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 18
        - cd ../../ && npm ci
    build:
      commands:
        - cd ../../ && npx turbo run build --filter=@bham/storybook
  artifacts:
    baseDirectory: storybook-static
    files:
      - '**/*'
  cache:
    paths:
      - ../../node_modules/**/*
